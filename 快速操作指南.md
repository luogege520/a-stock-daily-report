# ⚡ 快速操作指南（图文版）

## 🎯 目标
5 分钟完成从本地到 GitHub 的部署

---

## 📋 准备工作

### ✅ 需要准备的材料

| 项目 | 说明 | 获取方式 |
|------|------|---------|
| Git | 版本控制工具 | https://git-scm.com/download/win |
| GitHub 账号 | 代码托管平台 | https://github.com/signup |
| StepFun API Key | AI 模型密钥 | https://platform.stepfun.com/ |
| Gmail 邮箱 | 发送邮件 | https://mail.google.com/ |
| Gmail 授权码 | 邮箱授权 | https://myaccount.google.com/apppasswords |

---

## 🚀 操作步骤

### 第 1 步：安装 Git（如果未安装）

#### Windows 用户：

**方法一：官方安装包**
```
1. 访问：https://git-scm.com/download/win
2. 下载 64-bit Git for Windows Setup
3. 双击运行，一路点击 "Next"
4. 安装完成后，重启命令行
```

**方法二：winget 命令（Windows 10/11）**
```powershell
winget install --id Git.Git -e --source winget
```

**验证安装**：
```bash
git --version
# 应该显示：git version 2.x.x
```

---

### 第 2 步：配置 Git（首次使用）

打开命令行（PowerShell 或 CMD），输入：

```bash
# 设置用户名（替换为你的 GitHub 用户名）
git config --global user.name "YOUR_USERNAME"

# 设置邮箱（替换为你的 GitHub 邮箱）
git config --global user.email "your_email@example.com"

# 验证配置
git config --global --list
```

---

### 第 3 步：初始化 Git 仓库

```bash
# 进入项目目录
cd D:\阶跃AI\a-stock-daily-report

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 创建初始提交
git commit -m "Initial commit: A股晚间复盘报告自动生成系统"
```

**预期输出**：
```
Initialized empty Git repository in D:/阶跃AI/a-stock-daily-report/.git/
[main (root-commit) xxxxxxx] Initial commit: A股晚间复盘报告自动生成系统
 19 files changed, xxxx insertions(+)
```

---

### 第 4 步：创建 GitHub 仓库

#### 4.1 登录 GitHub
访问：https://github.com/

#### 4.2 创建新仓库
点击右上角 "+" → "New repository"

或直接访问：https://github.com/new

#### 4.3 填写仓库信息

```
Repository name: a-stock-daily-report
Description: A股晚间复盘报告自动生成系统
Public/Private: 选择 Public 或 Private

⚠️ 重要：不要勾选以下选项：
❌ Add a README file
❌ Add .gitignore
❌ Choose a license
```

#### 4.4 点击 "Create repository"

---

### 第 5 步：推送代码到 GitHub

创建仓库后，GitHub 会显示命令，复制并执行：

```bash
# 关联远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/a-stock-daily-report.git

# 切换到 main 分支
git branch -M main

# 推送代码
git push -u origin main
```

**如果提示登录**：
- 用户名：你的 GitHub 用户名
- 密码：使用 Personal Access Token（见下方）

#### 生成 Personal Access Token

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 设置：
   - Note: `a-stock-daily-report`
   - Expiration: `No expiration`（或选择有效期）
   - 勾选：`repo`（完整的仓库访问权限）
4. 点击 "Generate token"
5. **复制 token**（只显示一次！）
6. 在推送时，使用 token 作为密码

---

### 第 6 步：配置 GitHub Secrets

#### 6.1 进入仓库设置
```
你的仓库页面 → Settings → Secrets and variables → Actions
```

#### 6.2 添加 Secrets

点击 "New repository secret"，依次添加以下 6 个：

---

#### Secret 1: STEPFUN_API_KEY

```
Name: STEPFUN_API_KEY
Secret: sk-xxxxxxxxxxxxxxxxxxxxx
```

**获取方式**：
1. 访问：https://platform.stepfun.com/
2. 注册/登录
3. 进入 API Keys 页面
4. 创建新 Key
5. 复制 Key（格式：sk-xxxxx）

---

#### Secret 2: SENDER_EMAIL

```
Name: SENDER_EMAIL
Secret: your@gmail.com
```

---

#### Secret 3: SENDER_PASSWORD

```
Name: SENDER_PASSWORD
Secret: xxxx xxxx xxxx xxxx
```

**Gmail 授权码获取**：

**方法一：快速链接**
```
1. 访问：https://myaccount.google.com/apppasswords
2. 选择"邮件"和"其他设备"
3. 点击"生成"
4. 复制 16 位授权码
```

**方法二：手动操作**
```
1. 访问：https://myaccount.google.com/security
2. 开启"两步验证"
3. 返回安全性页面
4. 找到"应用专用密码"
5. 生成密码
```

**QQ 邮箱授权码**：
```
1. 登录 QQ 邮箱
2. 设置 → 账户
3. 开启 SMTP 服务
4. 生成授权码
```

---

#### Secret 4: RECIPIENT_EMAIL

```
Name: RECIPIENT_EMAIL
Secret: recipient@example.com
```

可以是任意邮箱（Gmail、QQ、163 等）

---

#### Secret 5: SMTP_SERVER

```
Name: SMTP_SERVER
Secret: smtp.gmail.com
```

**常用邮箱 SMTP 服务器**：
- Gmail: `smtp.gmail.com`
- QQ: `smtp.qq.com`
- 163: `smtp.163.com`
- Outlook: `smtp.office365.com`

---

#### Secret 6: SMTP_PORT

```
Name: SMTP_PORT
Secret: 587
```

**常用邮箱 SMTP 端口**：
- Gmail: `587`
- QQ: `587`
- 163: `465` 或 `25`
- Outlook: `587`

---

### 第 7 步：启用 GitHub Actions

```
1. 点击仓库顶部的 "Actions" 标签
2. 点击 "I understand my workflows, go ahead and enable them"
3. 看到 "Daily A-Stock Report" workflow
```

---

### 第 8 步：测试运行

```
1. 点击 "Daily A-Stock Report"
2. 点击右侧 "Run workflow"
3. 选择 "main" 分支
4. 点击绿色 "Run workflow" 按钮
5. 等待运行完成（约 2-5 分钟）
```

#### 查看运行状态

```
✅ 绿色勾号 = 成功
❌ 红色叉号 = 失败（点击查看日志）
🟡 黄色圆圈 = 运行中
```

#### 查看运行日志

```
1. 点击运行记录
2. 点击 "generate-and-send-report"
3. 展开每个步骤查看详细日志
```

#### 下载报告

```
1. 滚动到页面底部
2. 找到 "Artifacts" 部分
3. 点击下载 "daily-report-xxx"
```

#### 检查邮箱

```
1. 打开收件邮箱
2. 查找 "A股晚间复盘报告" 邮件
3. 如果没有，检查垃圾邮件文件夹
```

---

## ✅ 完成检查

部署完成后，确认以下项目：

- [ ] Git 已安装并配置
- [ ] 代码已推送到 GitHub
- [ ] 6 个 Secrets 已配置
- [ ] GitHub Actions 已启用
- [ ] 测试运行成功（绿色 ✓）
- [ ] 可以下载报告文件
- [ ] 邮箱收到报告

---

## 🎉 部署成功！

现在，系统将：
- ⏰ 每个交易日晚上 21:00 自动运行
- 📧 自动发送报告到你的邮箱
- 📁 自动保存报告（保留 30 天）

---

## ❓ 遇到问题？

### 问题 1: Git 推送失败

**错误信息**：
```
Authentication failed
```

**解决方案**：
使用 Personal Access Token 作为密码，不是 GitHub 登录密码。

**步骤**：
1. 生成 Token：https://github.com/settings/tokens
2. 勾选 `repo` 权限
3. 复制 Token
4. 推送时使用 Token 作为密码

---

### 问题 2: Actions 运行失败

**错误信息**：
```
Secret STEPFUN_API_KEY not found
```

**解决方案**：
检查 Secrets 配置。

**步骤**：
1. Settings → Secrets and variables → Actions
2. 确认所有 6 个 Secrets 都已添加
3. 检查名称拼写（区分大小写）
4. 重新运行 Workflow

---

### 问题 3: 邮件发送失败

**错误信息**：
```
Authentication failed
```

**解决方案**：
检查邮箱授权码。

**步骤**：
1. 重新生成邮箱授权码
2. 确认 SMTP 服务器和端口正确
3. 检查邮箱是否开启 SMTP 服务
4. 更新 Secrets 中的 SENDER_PASSWORD

---

### 问题 4: 报告生成失败

**错误信息**：
```
API key invalid
```

**解决方案**：
检查 StepFun API Key。

**步骤**：
1. 登录 StepFun 平台
2. 检查 API Key 是否正确
3. 确认额度是否充足
4. 更新 Secrets 中的 STEPFUN_API_KEY

---

## 📞 获取帮助

### 文档资源
- [完整部署指南.md](完整部署指南.md) - 详细步骤
- [QUICKSTART.md](QUICKSTART.md) - 快速开始
- [CHECKLIST.md](CHECKLIST.md) - 检查清单

### 在线帮助
- GitHub Issues - 提交问题
- GitHub Discussions - 讨论交流

---

## 🎁 额外提示

### 修改运行时间

编辑 `.github/workflows/daily-report.yml`：

```yaml
schedule:
  - cron: '0 13 * * 1-5'  # UTC 13:00 = 北京时间 21:00
```

**时间转换**：
- 北京时间 = UTC + 8 小时
- 北京时间 21:00 = UTC 13:00
- 北京时间 20:00 = UTC 12:00
- 北京时间 22:00 = UTC 14:00

### 添加多个收件人

修改 `RECIPIENT_EMAIL` Secret：

```
email1@example.com,email2@example.com,email3@example.com
```

用逗号分隔多个邮箱。

---

**祝你投资顺利！** 📈

**文档版本**：v1.0.0  
**最后更新**：2026-01-27
